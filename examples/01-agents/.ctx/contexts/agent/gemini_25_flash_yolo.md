@comment {
    _1: "This context sets the default parameters for all subsequent tags, "
    _2: "we're using it to specify provider once for all.                  "
} 

@set {
    provider: 'gemini -y -m gemini-2.5-flash'
}


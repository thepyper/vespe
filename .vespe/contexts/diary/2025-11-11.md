@set { provider: 'gemini -y -m gemini-2.5-flash' }

In README.md, voglio che aggiungi una nuova sezione "Templating with Handlebars", dopo la sezione "Core Tags", che
spieghi bene che tutti i context sono template handlebars;
spiega anche (sottosezione) che esistono le variabili speciali:
-   e seguenti = argomenti command line posizionali
-  = tutti gli argomenti space-separated
-  = input stdin

<!-- answer-99e3e5b4-958f-43be-829d-b66f4d43bcf8:begin { provider: 'gemini -y -m gemini-2.5-flash' }  -->
I will add a new section "Templating with Handlebars" to `README.md` after the "Core Tags" section. This new section will explain that all contexts are Handlebars templates and will include a subsection detailing the special variables ``, ``, and ``.

First, I'll read the `README.md` file to locate the insertion point.
I have the content of `README.md`. I will now insert the "Templating with Handlebars" section before the "CLI Usage" section. I'll use the `replace` tool for this.
The new section has been added to `README.md`. Now, I'll commit the changes.
`README.md` is modified, but other changes are irrelevant. I'll stage and commit only `README.md`.
`README.md` is staged. Committing now.
Changes committed. Running `git status` to confirm.
The `README.md` file is no longer listed as modified, and the commit was successful. I have completed the request.
<!-- answer-99e3e5b4-958f-43be-829d-b66f4d43bcf8:end {}  -->

Ora voglio aggiungere, subito dopo il preambolo, prima di "How it works", il video gif che c'e' in doc\demo_in_vscode.gif

<!-- answer-19e60870-47fe-47ef-a2c2-86b9088d1b03:begin { provider: 'gemini -y -m gemini-2.5-flash' }  -->
The GIF has been added to `README.md` and the changes have been committed.
<!-- answer-19e60870-47fe-47ef-a2c2-86b9088d1b03:end {}  -->

Aggiungi una table of contents, prima della sezione Installation direi.

<!-- answer-2e328a2b-52af-4a3d-8f9f-fc9ec6b8fce7:begin { provider: 'gemini -y -m gemini-2.5-flash' }  -->
I will add a Table of Contents (TOC) to the `README.md` file, placing it before the "Installation" section. I will generate the TOC by extracting all headings (H2 and H3) from the document and formatting them as a linked list.

Here's the plan:
1.  **Generate TOC**: Parse the `README.md` content to identify all H2 and H3 headings. For each heading, create a markdown link with an appropriate anchor.
2.  **Insert TOC**: Use the `replace` tool to insert the generated TOC section immediately before the "## Installation" heading in `README.md`.
3.  **Commit Changes**: Commit the updated `README.md` file to the git repository.

First, I will generate the Table of Contents based on the current `README.md` content.
The Table of Contents has been added to `README.md`. I will now commit the changes.
`README.md` is staged. Committing now.
Changes committed. Running `git status` to confirm.
`README.md` is committed. Other changes are unrelated. Request complete.
<!-- answer-2e328a2b-52af-4a3d-8f9f-fc9ec6b8fce7:end {}  -->
 


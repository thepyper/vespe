Plan to refactor data_generator output filename:

1.  **Modify `save_labeled_example` function in `src/bin/data_generator/pipeline.rs`**:
    *   Add `use chrono::Local;` and `use chrono::Timelike;` to the file.
    *   Inside the `save_labeled_example` function, generate a timestamp string using `let timestamp = Local::now().format("%Y%m%d%H%M%S%f").to_string();` (adding milliseconds for uniqueness).
    *   Change the filename generation from `output_dir.join(format!("example_{:04}.json", example_index))` to `output_dir.join(format!("example_{}.json", timestamp))`.
    *   The `example_index` parameter will no longer be used for the filename, but can remain in the function signature if it's used elsewhere or for logging.

2.  **Update the call to `save_labeled_example` in `src/bin/data_generator/main.rs`**:
    *   The `example_index` argument (`i`) will still be passed to `save_labeled_example` as it's part of the function signature, but its value will not be used for the filename.

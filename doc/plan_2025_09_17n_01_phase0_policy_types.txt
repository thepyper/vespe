### Fase 0: Preparazione degli Enum

Questa fase introduce gli enum `PolicyType` e `ToolType` per migliorare la sicurezza dei tipi e eliminare le stringhe hardcoded.

#### 0.1. Creazione del File `src/llm/policy_types.rs`

*   **Azione**: Creare un nuovo file `src/llm/policy_types.rs`.
*   **Contenuto**:
    ```rust
    // src/llm/policy_types.rs
    #[derive(Debug, Clone, Copy, PartialEq, Eq)]
    pub enum PolicyType {
        Json,
        Xml,
        Sections,
        // Aggiungere altri tipi di policy qui se necessario
    }
    ```
*   **Motivazione**: Fornisce un modo tipizzato per indicare quale stile di formattazione è richiesto per l'output di un tool.

**CONTINUA NEL FILE: `doc/plan_2025_09_17n_02_phase0_tool_types.txt`**
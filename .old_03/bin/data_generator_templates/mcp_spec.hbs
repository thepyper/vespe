Available tools are:
{{#each tools}}
- {{this.name}}: {{this.description}}
{{/each}}

The TOOL_CODE block must be a single JSON object that follows the 'Model Context Protocol' specification.
Each tool call must be a JSON object with `type: 'tool_use'`.

Here is an example of a single call:
```json
{{{json_example}}}
```

Rules for MCP format:
- The output must be valid JSON.
- `role` must be `assistant`.
- `content` is a list containing `tool_use` type objects.
- Each `tool_use` must have a unique and simple `id` (e.g., 'call_1'), the tool's `name`, and an `input` object with the parameters.
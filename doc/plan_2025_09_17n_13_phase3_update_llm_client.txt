### Fase 3: Integrazione e Verifica

#### 3.1. Aggiornamento di `src/llm/mod.rs`

*   **Azione**: Assicurarsi che i nuovi moduli delle policy siano correttamente esposti o referenziati. (Già fatto nella Fase 0.3).
*   **Motivazione**: Mantiene la coerenza della struttura del modulo.

#### 3.2. Aggiornamento di `src/llm/llm_client.rs` (o dove le policy sono istanziate)

*   **Azione**: Modificare la logica di istanziazione delle policy per utilizzare i nuovi nomi (`JsonPolicy`, `XmlPolicy`, `SectionsPolicy`).
*   **Contenuto**:
    ```rust
    // src/llm/llm_client.rs
    use crate::llm::impls::{json_policy::JsonPolicy, xml_policy::XmlPolicy, sections_policy::SectionsPolicy};
    use crate::llm::markdown_policy::MarkdownPolicy;

    // ... (altri use)

    pub fn get_markdown_policy(policy_name: &str) -> Box<dyn MarkdownPolicy> {
        match policy_name {
            "json" => Box::new(JsonPolicy::new()),
            "xml" => Box::new(XmlPolicy::new()),
            "sections" => Box::new(SectionsPolicy::new()),
            _ => Box::new(JsonPolicy::new()), // Default
        }
    }
    // ... (resto del file)
    ```
*   **Motivazione**: Adatta il codice client ai nuovi nomi delle policy.

**CONTINUA NEL FILE: `doc/plan_2025_09_17n_14_phase3_compile_test.txt`**
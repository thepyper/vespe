Plan to add logging for Ollama interactions in data_generator:

1.  **Initialize `tracing` in `src/bin/data_generator/main.rs`**:
    *   Add `use` statements: `use tracing_subscriber::{fmt, EnvFilter};`, `use tracing_subscriber::prelude::*;`, `use tracing_appender::rolling::{RollingFileAppender, Rotation};`, `use chrono::Local;`, `use std::path::PathBuf;`.
    *   Before `main` function, define a constant for the log directory: `const LOG_DIR: &str = ".vespe/log";`.
    *   Inside `main`, before any other logic, set up the tracing subscriber:
        ```rust
        let file_name = format!("data_generator_{}.log", Local::now().format("%Y%m%d%H%M%S"));
        let log_path = PathBuf::from(LOG_DIR).join(file_name);
        let file_appender = RollingFileAppender::new(Rotation::HOURLY, LOG_DIR, log_path);
        let (non_blocking_file_writer, _guard) = tracing_appender::non_blocking(file_appender);

        tracing_subscriber::registry()
            .with(fmt::layer().with_writer(std::io::stdout).with_filter(EnvFilter::from_default_env()))
            .with(fmt::layer().with_writer(non_blocking_file_writer).with_filter(EnvFilter::from_default_env()))
            .init();
        ```
    *   Ensure `EnvFilter::from_default_env()` is configured to allow `debug` level logs (e.g., by setting `RUST_LOG=debug` environment variable).

2.  **Add `tracing` macros in `src/bin/data_generator/ollama_client.rs`**:
    *   Add `use tracing::{info, debug};`.
    *   Inside `query_ollama` function:
        *   Before `let response = client...` line, add: `debug!("Ollama Request: model={}, system={:?}, prompt={}", model, system, prompt);`
        *   After `let response_body = response.json::<OllamaGenerateResponse>().await?;` line, add: `debug!("Ollama Response: {}", response_body.response);`

3.  **Update `Cargo.toml`**: Ensure `tracing-appender` is listed as a dependency. (It already is, but good to double check).